/**
 * Processing module exports
 * OLD CODE - COMMENTED OUT FOR NEW ARCHITECTURE
 */
/*

export { readFileUtf8, isDirectory, findDataFiles } from './fileUtils';

// JSON processing now unified
export { JsonProcessor } from '../core/json/processor';

// Legacy exports for backward compatibility (delegate to JsonProcessor)
import { JsonProcessor } from '../core/json/processor';
export const parseJsonSafe = JsonProcessor.parse;
export const isJsonLike = JsonProcessor.isJsonLike;
*/
